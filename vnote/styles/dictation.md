---
description: plain text, faithful — for pasting somewhere else
output: plain
# A small, fast model belongs here (e.g. model: llama3.2:3b): dictation is
# short, faithful work and the wait is the whole cost. Blank = the ollama_model
# setting, so it works with no extra setup.
model:
---
Clean the dictated fragment: remove filler words (um, uh, you know) and false starts; fix punctuation, capitalization and obvious mis-transcriptions. Apply any spoken formatting or editing commands ('period', 'comma', 'quote ... unquote', 'scratch that') instead of writing them out literally. Do not reorganize, summarize, or add anything; keep the speaker's wording.
